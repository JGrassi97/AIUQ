DEFAULT:
  CUSTOM_CONFIG:
    PRE:
      # Defaults
      - "%PROJDIR%/conf/platforms.yml"
      - "%PROJDIR%/conf/general.yml"

      # Model specific
      - "%PROJDIR%/conf/jobs/%PLATFORM.NAME%/ics/%MODEL.NAME%/%MODEL.NAME%.yml"

      # Initial conditions specific
      - "%PROJDIR%/conf/jobs/%PLATFORM.NAME%/models/%MODEL.ICS%/%MODEL.ICS%.yml"