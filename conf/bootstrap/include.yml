DEFAULT:
  CUSTOM_CONFIG:
    PRE:
      # Defaults
      - "%PROJDIR%/conf/platforms.yml"
      - "%PROJDIR%/conf/general.yml"

      # Model specific
      - "%PROJDIR%/conf/models/%MODEL.NAME%/%MODEL.NAME%.yml"

      # Initial conditions specific
      - "%PROJDIR%/conf/ics/%MODEL.ICS%/%MODEL.ICS%.yml"