DEFAULT:
  CUSTOM_CONFIG:
    PRE:
      # Defaults
      - "%PROJDIR%/conf/platforms.yml"
      - "%PROJDIR%/conf/general.yml"

      # Model specific
      - "%PROJDIR%/conf/jobs/%PLATFORM_NAME%/%MODEL.NAME%/%MODEL.NAME%.yml"

      # Initial conditions specific
      - "%PROJDIR%/conf/jobs/%PLATFORM_NAME%/%MODEL.ICS%/%MODEL.ICS%.yml"